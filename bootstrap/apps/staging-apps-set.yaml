apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: staging-apps
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        generators:
          - git:
              repoURL: git@github.com:lowc1012/argo-lab.git
              revision: HEAD
              directories:
                - path: "apps/*"
              pathParamPrefix: team
          - merge:
              generators:
                - clusters:
                    selector:
                      matchLabels:
                        env: staging
                    values:
                      selector: '{{index .metadata.labels "team"}}'
                - git:
                    repoURL: git@github.com:lowc1012/argo-lab.git
                    revision: HEAD
                    directories:
                      - path: "apps/{{.team.path.basename}}/staging/*"
                    pathParamPrefix: app
                    values:
                      selector: '{{.team.path.basename}}'

  template:
    metadata:
      name: "{{.name}}-{{.values.selector}}-{{.app.path.basename}}"
      labels:
        environment: staging
        team: "{{.values.selector}}"
    spec:
      project: "{{.values.selector}}"
      source:
        repoURL: git@github.com:lowc1012/argo-lab.git
        path: "apps/{{.values.selector}}/staging/{{.app.path.basename}}"
        targetRevision: HEAD
      destination:
        name: "{{.name}}"
        namespace: argocd
      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
          selfHeal: true
        retry:
          backoff:
            duration: 1m
          limit: 100
