apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: staging-apps
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - matrix:
        generators:
          - git:
              repoURL: git@github.com:lowc1012/argo-lab.git
              revision: HEAD
              pathParamPrefix: "team"
              directories:
                - path: 'teams/*'

          - git:
              repoURL: git@github.com:lowc1012/argo-lab.git
              revision: HEAD
              directories:
                - path: 'apps/{{team.path.basename}}/staging/*'
              values:
                env: staging
                app: '{{path.basename}}'

  template:
    metadata:
      # staging-team-a-nginx
      name: '{{.values.env}}-{{team.path.basename}}-{{.values.app}}'
      environment: '{{.values.env}}'
    spec:
      project: '{{team.path.basename}}'
      source:
        repoURL: git@github.com:lowc1012/argo-lab.git
        path: 'apps/{{team.path.basename}}/{{.values.env}}/{{.values.app}}'
        targetRevision: HEAD
      destination:
        name: in-cluster
        namespace: '{{.values.app}}'
      syncPolicy:
        syncOptions:
          - CreateNamespace=true 
        automated:
          prune: true
          selfHeal: true
        retry:
          backoff:
            duration: 1m
          limit: 100
